# Server Port Configuration
PORT=3000

# JSON Web Token Secret (used for signing user sessions)
JWT_SECRET=wellness_hub_jwt_super_secret_key_12345

# Database Configuration (for local MySQL connection)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=wellness_db

# AI Coach Keys (Gemini is preferred and injected automatically by AI Studio)
GEMINI_API_KEY=
OPENAI_API_KEY=
